import { ListaProps } from '../../common/ListaProps/ListaProps';

export const Saludo = () => {
  return (
    <div>
      <ListaProps saludo="¡Hola desde el componente Saludo!" />
    </div>
  );
};